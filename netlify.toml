[build]
  command = "rm -rf node_modules && npm install --legacy-peer-deps && node dependency-fixer.js && npm run build"
  publish = "dist"
  ignore_warnings = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
